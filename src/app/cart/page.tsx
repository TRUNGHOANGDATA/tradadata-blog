import CartPageClient from './CartPageClient';

export const metadata = {
    title: 'Giỏ hàng | Trà Đá Data',
    description: 'Xem lại giỏ hàng và tiến hành thanh toán.',
};

export default function CartPage() {
    return <CartPageClient />;
}
